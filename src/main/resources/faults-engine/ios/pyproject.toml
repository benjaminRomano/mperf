[project]
name = "ios-fault-visualizer"
version = "0.1.0"
description = "Collect and visualize iOS startup page-fault patterns"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.1.0",
]

[tool.black]
line-length = 88
target-version = ["py311"]
